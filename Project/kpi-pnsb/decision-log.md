# 🗂️ PNSB KPI — Decision Log

*Current decision state, blockers, and architecture choices for the PNSB BSC KPI Performance Management System. Companion to `planning.md`, `flow-diagram.md`, `db-design.md`, `weight-calculation.md`, `watchouts.md`.*

Project folder: `C:\laragon\www\Project-AI-MemoryCore\Project\kpi-pnsb\`
Key files: `planning.md`, `flow-diagram.md`, `db-design.md`, `files/kpi1-excel-data.md` (Excel readable export), `files/` (Excel + PDF + bell curve image)

**Company**: PNSB = Permodalan Negeri Selangor Berhad
**System**: BSC KPI Performance Management System (Laravel 13 + Livewire 4 + Tailwind + MySQL)
**Reference system**: AIROD PMS+ (airod.pmsplus.my) — used as UI/flow reference only. PNSB diverges on scoring scale (% not 1–5), weight locking (ALP ketetapan), and a flexible review-period cycle (default half-yearly — see Appraisal Cycle Mechanics below; SUPERSEDES the old "annual-only" assumption).

## Remediation SHIPPED — all 4 priorities merged into `dev` (2026-06-30)

The 18-finding review (see next section) was fixed in **4 issue→branch→PR slices, all merged into `dev`**:

| Priority | Findings | Issue | PR (merged) | Branch |
|---|---|---|---|---|
| **P0** privilege escalation | A1–A6 | #5 | **#6** | `fix/authz-tier-guard` |
| **P1** wrong-grade integrity | F1, F2, F4, S3 | #7 | **#8** | `fix/appraisal-integrity` |
| **P2** moderation integrity | M1, M3, M4, M5, M6 | #9 | **#10** | `fix/moderation-integrity` |
| **P3** robustness & hygiene | F3, F5, EditPeriods txn, audit LIKE, S6 | #11 | **#12** | `fix/robustness` |

**17 of 18 fixes shipped.** Test suite grew **258 → 280 green**; Pint clean throughout.

**Key implementation notes (so we don't re-derive them):**
- **P0** — role logic lives on the **`User` model** (constants + helpers `isSuperAdmin()`, `assignableRoles()`, `canManage()`, `isSystemRole()`), NOT an enum/new folder (Amirul's convention — I initially used an `app/Enums/Role.php` and he corrected it). `Gate::before` super-admin shortcut in `AppServiceProvider`. Roles/Permissions routes → super-admin only.
- **P1** — extracted **`Scorecard::weightBlockers(?array $overrideWeights)`** (shared by employee submit + manager override, fixes F1). F2 blocks review when competency-based card has no competency lines. S3 gates `kpi_score` write behind `allPeriodsScored()`. F4 = warn at scorecard generation about appraiser-less (null-manager non-CEO) employees (dropped the more invasive "block head-less dept assignment" — broke 3 Org tests, forces head-first setup).
- **P2** — **`ModerationLog::record()` reloads `lockForUpdate()` inside one transaction** (fixes M1 stale-grade race + M5 atomicity together; signature unchanged so all 9 callers untouched). New **`Scorecard::MODERATION_STATUSES`** const drives both pools (M6). `lockGrades` blocks on ungraded cards (M4) + re-reads cycle `lockForUpdate` (M3).
- **P3** — audit search uses **`LIKE ? ESCAPE '='`** (portable MySQL+SQLite, verified). EditPeriods clamps `finalIndex` + wraps delete/recreate in a transaction.

**DELIBERATELY NOT FIXED — S4** (`BscPerspective::rescaleKpiWeights` rounding drift): its docblock documents WHY exact-sum reconciliation is avoided (a perspective holds multi-level KPIs summing to a *multiple* of besar), and per-scorecard exactness is already handled by `Scorecards/Show::suggestedWeights` (last KPI absorbs remainder). Forcing the sum risks a real regression → **flagged for Amirul's product decision**, not fixed.

**Refuted (verified false, untouched):** S1 (tier scoring is continuous % by design), S2 (per-period weighted_score quirk inert), S5 (kpiReviewerAdjusted undercount affects both sides equally).

**Follow-ups parked:** (a) S4 product decision; (b) **flaky `DashboardTest::test_admin_dashboard_shows_department_and_category_widgets`** — non-deterministic (faker-sequence-sensitive data; passes in isolation, failed once in full suite then passed on re-run). Pre-existing, not remediation-caused; make its data explicit. Full plan in `docs/security/REMEDIATION-PLAN.md` (in the kpi_pnsb repo).

## Security Code Review + Remediation Plan (2026-06-30, doc-only — no code changed)

Ran a **high-effort multi-agent `/code-review`** over the whole `dev` app (`dev` = entire app vs near-empty `main`, ~7.4k lines of Livewire/Models). 7 parallel finders → dedup → 7 adversarial verifiers. **18 findings verified, 3 refuted.** Wrote the execution guide to **`docs/security/REMEDIATION-PLAN.md`** in the kpi project (first file under new `docs/security/`). **No app code touched** — planning only.

**Locked decisions for the eventual fixes:**
- **hr-admin** keeps managing regular users but is **blocked from the super-admin tier**; role + permission editing becomes **super-admin only**.
- **`Scorecard.kpi_score`** is persisted **only when all review periods are scored** (mid-cycle stays null → UI shows "in progress").

**The headline bug — P0 privilege escalation (A1–A6, all CONFIRMED):** routes admit `super-admin|hr-admin` but the Livewire **action methods have NO tier check**, and there's **no `Gate::before`/policy** (`AppServiceProvider::boot()` empty). An hr-admin can self-grant super-admin (`Users/Edit:95`), mint one (`Users/Create:59`), mass-assign permissions (`Roles/Edit:90`), delete core permissions (`Permissions/Index:78`), delete/deactivate a super-admin (`Users/Show:32`), or delete the super-admin role (`Roles/Index:93`). Fix = central `app/Enums/Role.php` + `Gate::before` super-admin shortcut + tighten Roles/Permissions routes to super-admin only + per-action `abort_unless`/role-assignment allowlist + a brand-new `AuthorizationTierTest` (these components currently have **zero** authz tests).

**P1 wrong-grade integrity:** F1 override skips weight-sum recheck (`Appraisals/Show:166`); F2 competency-based card with no generated lines scores competency 0 (`:436`); F4 null-manager non-CEO has no appraiser → stalls cycle (`:136`); S3 partial `kpi_score` persisted/shown mid-cycle (`Results:320`). **P2 moderation:** M4 ungraded card orphaned when cycle closes + M6 pool missing `status='moderation'` filter (`Moderation/Index:47-50`); M5 `ModerationLog::record()` not atomic; M1/M3 concurrency races (PLAUSIBLE). **P3 hygiene:** F3 `Setup::save()` no status recheck; F5 `finalIndex` unvalidated; EditPeriods delete+recreate not transactional; audit search LIKE `%_` unescaped; S4 rescale rounding drift; S6 preview clamp `[0,5]` vs live `[1,5]`.

**Reuse pointers found:** extract `Scorecard::weightBlockers()` from `Show::submitBlockers():503-536` (reused by F1); `DB::transaction` style at `Moderation/Index::lockGrades():108`; existing `allPeriodsScored()` for S3; `abort_if` precedent `Users/Show:32`; test helper `userWithRole()` at `CalculationSettingsTest:24-31`.

**3 REFUTED (don't re-chase):** S1 — tier scoring is **continuous achievement % by design**, not 0/60/80/100 bands (threshold/meet/stretched are free-text reference). S2 — per-period `weighted_score` semantic quirk is inert (nothing sums those rows). S5 — `kpiReviewerAdjusted` undercount hits both sides equally, flag doesn't misfire.

**Next when we proceed to fix (standing workflow):** 4 GitHub issues + 4 `fix/*` branches off `dev`, P0 first (`fix/authz-tier-guard`). Full plan + finding index in `docs/security/REMEDIATION-PLAN.md`. Suite is **258 green** today — must stay green.

## Calculation Settings Module (2026-06-29, branch `feat/calculation-settings`, issue #1)

New admin page `/calculation-settings` (super-admin | hr-admin), inspired by AIROD PMS+ "Calculation Settings". Surfaces scoring config that previously lived only in seeders/constants. Files: `app/Livewire/Settings/Calculation.php` + `resources/views/livewire/settings/calculation.blade.php` + route in `routes/web.php` + sidebar link. Tests: `tests/Feature/CalculationSettingsTest.php` (10).

**4 panels:**
1. **Score Weight Settings** — edit the 3 category templates' KPI/Competency split (`scorecard_templates`), hard 100%-per-row rule.
2. **BSC Dimensions** — edit weight/description/sort_order of the fixed 4 perspectives (`bsc_perspectives`). **Add/Delete removed** (Amirul: keep at 4). Weight editing now reuses the KPI Library's rescale + 100% gate — see `weight-calculation.md` "TWO editors of besar".
3. **Calculation Formula** — static reference panel.
4. **Calculation Preview Tool** — live what-if mirroring `Appraisals\Show::completeReview()`: category + KPI score + a **Likert matrix of the 5 core competencies** (reuses the appraisal-review pattern) → final score + grade. Plus a **Bell Curve Distribution** sub-panel: pick cycle + department/org-wide → real distribution vs target (reuses `livewire/moderation/_bellcurve.blade.php` UNCHANGED) with a live gold "what-if marker" overlaid OUTSIDE the partial's `wire:ignore`.

**Scope calls (Amirul):** skipped editable Rating Scale (would touch core `gradeForScore`) and per-department BSC weightages (conflicts with per-KPI weighting).

**Bug fixed same session:** BSC weight edits desynced the KPI Library (see weight-calculation.md). Fix = extract `BscPerspective::rescaleKpiWeights()`, both editors use it, enforce 100%.

**Status:** all on branch `feat/calculation-settings`, committed against #1, NOT yet merged. PR into `dev` not opened yet. `gh` CLI installed this session (`C:\Program Files\GitHub CLI\gh.exe`, authed as Amirul1asyraff). Full suite 244 green.

## Architecture Decisions (Locked)

- **Employee categories & scoring split** (ALP ketetapan — cannot change without new Board resolution):
  - KPE (CEO): 100% KPI
  - Eksekutif → Pengurusan Kanan: 80% KPI + 20% Competency (Penilaian Keperibadian)
  - Sokongan / Teknikal: 100% Competency
- **BSC weights**: Financial 40%, Customer 30%, Internal 20%, Learning 10%
- **KPI tiers**: 3-tier — Ambangan (Threshold) / Setuju (Meet Target) / Lebihan (Stretched)
- **Tier score formula (C7)**: Threshold=60, Meet Target=80, Stretched=100, Below=0. Hard cap at 100. Skor = (Pencapaian Sebenar / 100) × KPI Weight. Confirmed from PNSB Excel.
- **Grade boundaries (C5)**: Cemerlang 90–100 / Sangat Baik 76–89 / Baik 60–75 / Memuaskan 50–59 / Perlu Diperbaiki < 50
- **Manager score override (C1)**: Manager directly overrides both KPI and Competency scores submitted by employee — no rejection flow, no sending back. Manager edits score in-system. Staff self-rating is reference only. Manager is final authority.
- **Moderation**: 2-round bell curve — MOD1 by HR, MOD2 by KPE. 91 staff total.
- **Bell curve = YEAR-END ONLY (Locked 2026-06-25, Amirul)**: applied ONCE to the FINAL COMPOSITE score (which already rolls up every review period's weighted marks: e.g. Q1×25 + Q2×25 + Q3×25 + Q4×25). NOT per review period — curving each period then averaging is mathematically unsound and fragments the distribution. Managers still SCORE each period (recorded per period with its weight) but moderation/bell curve fires only at the `is_final` period on the composite. **Keeps schema as-is**: `bell_curve_targets` + `moderation_logs` stay CYCLE-scoped — NO `review_period_id` needed on them.
- **Terminology**: Malay label first, English in brackets. KPI = Petunjuk Prestasi Utama, Competency = Kompetensi / Penilaian Keperibadian.
- **Role architecture**: Hierarchy-driven via `manager_id` chain.
- **ALP weight locking (C3)**: KPI/Competency weights are ALP ketetapan — only Super Admin can update, only after new ALP resolution.
- **5 system roles — NOT 4 (Locked 2026-06-27, Amirul)**: `super-admin`, `hr-admin`, `executive-director` (KPE), `division-head` (Ketua Bahagian), `staff`. `division-head` is a real distinct role — it's BOTH a reviewer (approves its dept's staff, `view-dept-scorecards`) AND a reviewee (own 80/20 scorecard reviewed by KPE); it's what makes the `manager_id` appraisal chain work (Eksekutif & Sokongan report to a Ketua Bahagian). Defined in `RolePermissionSeeder.php:51-89`. NOTE on category vs role: `division-head` (HOD) and `staff` (Eksekutif) are DIFFERENT roles but share the SAME scoring **category `executive`** (80% KPI / 20% Competency) — scoring identical, permissions differ.
- **HR + Super Admin are NOT assessed (Locked 2026-06-27, Amirul — keep current behaviour)**: `hr-admin` and `super-admin` are **administrative roles**, never get a scorecard. Rationale = **separation of duties**: HR runs the cycle, MOD1 moderation, and locks ALL final grades — whoever holds the "lock all grades" button must not also be a grade being locked in the same system. Already enforced: `Cycles\Show::eligibleEmployees()` generates scorecards only for `APPRAISAL_ROLES = [executive-director, division-head, staff]` and excludes `ADMIN_ROLES = [super-admin, hr-admin]` via `whereDoesntHave`. **Backlog (NOT now):** if PNSB ever wants real HR *department people* appraised, model them as ordinary employees (HR head = `division-head`, HR staff = `staff`) under a Human Resources department, reviewed via `manager_id` — kept SEPARATE from the `hr-admin` admin account. That's org-modelling, not a change to whether the admin role gets a scorecard.

## Appraisal Cycle Mechanics (Locked — 2026-06-25)

- **Cycle = annual container, ONE per year** (`appraisal_cycles.year` UNIQUE). Status enum: `draft → active → appraisal → moderation → closed`. Opened by **HR Admin** (`hr-admin`, `manage-cycles`). HR runs the cycle; not in the appraisal chain.
- **Option B — cycle status is a GATE (traffic light), NOT a bulldozer.** Advancing the cycle status only opens/closes phase doors org-wide; it NEVER auto-pushes individual scorecards. Each scorecard advances on its own when its people act. No `foreach scorecard` bulk-update on cycle transitions — the cycle controller just updates its own row + logs. Cycle = referee, scorecard = player.
- **Universal gate rule** — every scorecard action allowed ONLY if: (1) cycle in matching phase, (2) scorecard in correct prior status, (3) actor has permission + relationship, (4) the review period being scored is OPEN.
- **Cycle = forward-only.** No org-wide backward step (e.g. `appraisal → active`). Instead → **per-scorecard reopen exception**: HR reopens ONE scorecard back a step (e.g. `locked → appraisal`), logged to `scorecard_status_logs` with who/when/reason. Scalpel, not sledgehammer.
- **Scorecard generation = SEPARATE button inside `draft`** (distinct from activation). HR previews the roster, then generates one scorecard per active employee in the appraisal chain (`executive-director`, `division-head`, `staff`), EXCLUDING `super-admin`/`hr-admin`. Template picked by `users.category`. Idempotent (unique `user_id`+`cycle_id`).
- **New-hire mid-cycle**: HR manually adds a scorecard from the dashboard (re-running generation is idempotent and picks them up).
- **Readiness snapshot before advancing** *(REFINED 2026-06-29 — see "Phase-Gate Hardening" below)*: dashboard/modal shows scorecard counts per status. Entry into **appraisal = warn-but-allow** (laggards catch up during the phase). Entry into **moderation = HARD BLOCK** until every card is scored (it's the point of no return — all per-card work gates close). Empty cycle (0 scorecards) is blocked from advancing past `active`.
- **`draft` = setup / `active`+ = frozen structure (Locked).** Cycle STRUCTURE is fully editable only while `draft`; it FREEZES on activation because recorded scores depend on it. Once `active`: number of review periods (frequency), period labels/weights/`is_final`, and scorecard templates (category splits) are LOCKED — cannot reshape mid-cycle (e.g. can't switch 2→4 periods once scores exist). KPI/competency weights are ALP-locked (super-admin only) regardless of phase. Still editable while active = per-person operational only: add new-hire scorecard, reopen one scorecard, and adjust period window dates (`opens_at`/`closes_at`, scheduling only — doesn't touch data). One line: shape the cycle in draft; once live you can only nudge individuals, never reshape the machine.

## Phase-Gate Hardening + Bell Curve Summary (BUILT 2026-06-29, Amirul)

Two builds this session, both shipped green (234 tests, Pint clean, `npm run build`).

### Phase-gate hardening (the "stranded scorecards" fix)
- **Bug Amirul found**: a Support card sat in `draft` while the cycle was also `draft` and showed (with nothing to do) in the manager's Team Approvals + skewed the cycle's Draft counter. Then later, the CEO card stuck at `kpi_approved` and the HOD at `appraisal` after the cycle was pushed to `moderation` — both permanently stranded.
- **Root cause = two gate gaps**: (1) `advancePhase()` never checked scorecard readiness, so the cycle outran its cards (once `moderation`, every per-card work gate closes → stranded); (2) KPI submit/verify were NOT gated on cycle phase (unlike competency self-assessment), so KPI cards raced ahead while the cycle was still `draft`.
- **B2 — readiness guard** (`Cycles\Show::advancePhase()`): new `scorecardsReadyForPhase()` using `PHASE_MIN_STATUS = ['appraisal'=>'appraisal','moderation'=>'moderation']` + index compare on `Scorecard::STATUSES`. **Moderation entry = HARD BLOCK** if any card below `moderation`; **appraisal entry = warn-and-allow** (preserves the designed staggered catch-up — cards reach `appraisal` at different times, see `Appraisals\Show::reviewPhasePending()`); both require ≥1 scorecard. Modal shows a per-status breakdown (red+disabled for moderation, amber+enabled for appraisal).
- **B1 — phase-gate per-card KPI work**: `Scorecards\Show::canEdit()` + `Appraisals\Show::canVerify()` now block when cycle ∈ `{draft, moderation, closed}` (i.e. **allow active + appraisal**), mirroring `canSelfAssessCompetency()`. **CRITICAL nuance**: do NOT use a strict `=== 'active'` check — that would re-strand cards that legitimately catch up during the appraisal phase. The "not draft/moderation/closed" predicate is load-bearing.
- **Watch-out recorded** in `watchouts.md` → "Phase-gate rules".

### Bell Curve Summary (the PNSB Excel deliverable)
- **What**: the official "BELL CURVE SUMMARY" — a 5-row table (grades worst→best) with columns **Sasaran · KB · MOD1 · MOD2 · % over target**, plus a chart overlaying those 4 series. Added to BOTH the Reports page and the Moderation overview.
- **Engine already met the requirement** — this was a pure presentation/reporting layer on data we already store. No engine/data changes.
- **Stage reconstruction (the key knowledge)**:
  - **KB** (raw, pre-moderation) = `Scorecard::earnedGrade()` (derived from `final_score`; moderation preserves the score, so earned grade = original). Fallback to stored `grade` if score null.
  - **MOD1** (after HR's first round) = the card's **latest MOD1 `after_grade`**, else KB (no MOD1 change). Reconstructed via `ModerationLog::latestByCardForRound($cycleId,'MOD1')` (one query, `orderBy('id')` then `keyBy('scorecard_id')` so last write wins — `id` is the tiebreaker, NOT `moderated_at`).
  - **MOD2** (final) = the card's **current `grade`** = existing `BellCurveTarget::distribution()`. (`record()` keeps `scorecard.grade` authoritative, so MOD2 always == current grade.)
  - **Sasaran** = `BellCurveTarget::targetsForHeadcount(N)`.
- **`% over target = MOD2 ÷ Sasaran`** per grade (decoded from the Excel: 13/7=186%, 52/61=85%, 24/16=150%, 2/3=67%). Guard divide-by-zero → render `—` when Sasaran is 0 (happens for rare bands at small headcount).
- **Code**: `BellCurveTarget::distributionBy(cards, gradeResolver)` (generalises `distribution()`) + `BellCurveTarget::summary(graded, mod1Map)` (returns targets/kb/mod1/mod2 + worst→best `rows` with `pctOverTarget` + reconciling `totals`). Wired via `#[Computed] bellCurveSummary()` on `Reports\Index` (uses `allScorecards`) and `Moderation\Index` (uses `scorecards`). View = new static partial `resources/views/livewire/moderation/_bellcurve_summary.blade.php` (4-line SVG reusing the monotone-cubic `smooth()` math, NO tween — leaves the live 2-line morph chart `_bellcurve.blade.php` untouched). Colors: Sasaran dark-dashed, KB blue, MOD1 amber, MOD2 green.
- **Pool**: same graded pool as moderation (includes the CEO). Excluding top management from the "KB" pool is OUT OF SCOPE (would desync from the moderation pool — separate decision).
- **MOD1 vs MOD2 recap** (for reference): MOD1 = HR, **per department**, first round; MOD2 = KPE/CEO, **org-wide**, second/final round. Both preserve the score and only move the grade band, logging every move with its round. Code switch: `Moderation\Session::round()` = `departmentId===null ? 'MOD2' : 'MOD1'`.

## Review Period Layer (Locked — flexible frequency; SUPERSEDES "annual-only")

- **Frequency is flexible via a review-period layer** so changing cadence later = a data change, NOT a code rewrite.
- **NEW table `review_periods`** (cycle hasMany): `cycle_id`, `sequence`, `label` ("Mid-Year"/"Year-End"/"Q1"…), `weight` (%), `is_final` (the period that triggers moderation + bell curve), `opens_at`/`closes_at`, `status` (`upcoming → open → scored → closed`).
- **Definitions set ONCE, scores repeat per period**: KPI/competency targets (weight/threshold/meet_target/stretched) stay on `scorecard_kpis`/`scorecard_competencies` (set once per year). A NEW per-period score layer records one score per line PER period.
- **Frequency = data**: 1 period = annual, 2 = half-yearly, 4 = quarterly. All logic loops over "this cycle's periods" — never hard-codes a count.
- **Default policy = half-yearly (2 periods)**: Mid-Year checkpoint + Year-End final. Moderation/bell curve fire ONLY on the `is_final` period; checkpoints record scores but don't trigger the curve.
- **Roll-up via the `weight` column** (keeps checkpoint-vs-weighted flexible, decide later): checkpoint style = mid-year `weight 0` / year-end `100` (year-end = grade); weighted style = e.g. 40/60 blended. Same roll-up logic reads either.

## KPI Library structure + Snapshot pattern (Locked 2026-06-25, Amirul)

- **Library hierarchy = 3 levels**: `bsc_perspectives` (fixed 4, ALP-locked weights) → **`strategic_objectives`** (NEW TABLE — belongs to a perspective; HR can ADD/REMOVE freely, e.g. Financial may have 5 objectives) → `kpis` (each KPI belongs to an objective). Matches the real PNSB Excel (`KPI1` sheet): columns No | BSC(+besar%) | Objektif Strategik | KPI | Pemberat Kecil | Pemberat Besar | Unit | Ambangan/Setuju/Lebihan | Pencapaian Sebenar(0/60/80/100 index) | Skor | Catatan. Display = grouped Perspective → Objective → KPI rows.
- **SNAPSHOT PATTERN (critical for yearly changes)**: the Library is the *current master* (edited every year). A **scorecard is a FROZEN COPY** taken when KPIs are set for that cycle → editing/adding/removing Library objectives/KPIs/weights next year NEVER changes past scorecards. This is WHY `scorecard_kpis` already stores its OWN kpi_weight/threshold/meet_target/stretched (snapshot, not live ref). TODO when building KPI Library/KPI-setting: also snapshot **strategic_objective + KPI title + unit** onto `scorecard_kpis` (currently only links by id) so a renamed/deleted Library row can't rewrite history.
- **Weight rule**: enforce KPI weights total **100%** (changeable if needed). Excel shows kecil weights sum to 100% overall; per-perspective sum to besar is NOT strictly enforced (Customer kecil=20 but besar=30 in real doc) — total-100 is the rule.
- **Pencapaian Sebenar** = the tier index (Below 0 / Ambangan 60 / Setuju 80 / Lebihan 100) → maps to `scorecard_kpis.tier_score`. `weighted_score` = tier_score/100 × kecil weight. Raw real value goes in `notes` (Catatan).

## KPI Library — BUILT 2026-06-25 (CRUD + UI)
- **MASTER-DETAIL (restructured 2026-06-25 per Amirul — index was too compact):** `Kpis/Index` (route `kpis.index`) = **2×2 grid** of the 4 perspective cards showing only name + weight% + objectives_count + kpis_count (`withCount(['strategicObjectives','kpis'])`), each links to → `Kpis/Show` (route `kpis.show` = `kpis/{perspectiveId}`). **All CRUD lives on Show** (scoped to ONE perspective): objectives → KPIs tree + modal CRUD (objective create/edit/delete, KPI create/edit/delete). Show guards every action with `findObjective()`/`findKpi()` that filter by `bsc_perspective_id = perspectiveId` (can't touch another perspective's rows). KPI create sets `bsc_perspective_id` = objective's perspective + `created_by` = auth. KPI fields: title, uom, level, category_scope, is_active. Both under `role:super-admin|hr-admin`, sidebar wired. `KpiLibraryTest` = 14 (relationships + index counts + objective/KPI CRUD + validation + perspective-isolation). `KpiLibrarySeeder` seeds samples. Full suite 105 green.
- **Small weight (Pemberat Kecil) is in the LIBRARY (decided 2026-06-25, Amirul):** `kpis.weight` decimal(5,2) default 0 + form field + decimal:2 cast + factory. Bounded by the perspective's BIG weight (besar) — Show page shows a live indicator "{sum}% / {besar}%" (green `weightMatchesPerspective()` / red) via `kpiWeightTotal()` = SUM of the perspective's KPI weights. Indicator WARNS, doesn't hard-block (flexibility). Seeded KPIs sum to each besar (Fin 20+20=40, Cust 15+15=30, Int 20, Learn 10). Unit field relabelled "Unit / Pengukuran" (the Excel "Unit" col = pengukuran/measurement; one field = `kpis.uom`). KPI **targets/standards (Ambangan/Setuju/Lebihan) + actuals stay on `scorecard_kpis`** (employee submits them) — NOT in the library.
- **Index design polish:** Index = **2×2 grid** (`grid-cols-2`) of perspective cards (accent bar + editable description + objectives/KPIs counts) ABOVE 4 summary stat cards (Perspectives / Objectives / KPIs / Total Weight via `metrics()` computed). `description` is a real `bsc_perspectives.description` column (accessor falls back to `DESCRIPTIONS` const; edit via pencil modal on Show). Customer perspective colour changed navy→**blue-600** (`BscPerspective::COLORS`; safelisted in tailwind.config.js). Show paginates objectives (8/page).
- **Weight is INLINE-EDITABLE in the Show table (refactored 2026-06-25):** removed weight from the KPI modal (new KPIs start at 0%); weight column = `<input wire:model.live="kpiWeights.{id}">` bound to a `kpiWeights` array (loaded in mount). Live total indicator + **"Save Weights"** button → `saveWeights()` batch-validates total ≤ besar (enforced) then persists. This replaced the old per-KPI hard cap (which blocked adding when full) — now you add a KPI then rebalance all weights together. `weightMatchesPerspective()` uses `liveWeightTotal()`.
- **KPI scoped by LEVEL + CATEGORY only (CONFIRMED final 2026-06-25, Amirul — requirement):** department/designation targeting was added then **REMOVED** (dropped `kpis.department_id`/`designation_id` columns, model relations, form selects, table columns, validation, seeder logic). A KPI has just `level` (company/department/individual) + `category_scope`. **Category options = null(all) / ceo / executive ONLY — NO "support"** because per the ALP 80/20 rule support is 0% KPI / 100% competency, so support staff have NO KPIs. Validation `in:ceo,executive`. The KPI/competency split itself lives on ScorecardTemplate per category (ceo 100/0, executive 80/20, support 0/100).
- **Show KPI display = TABLE per objective** (was cards/list): columns No. | KPI | Weight(editable) | Unit | Level | Category | Status(Active/Inactive) | Action. Null category shows muted "All". `overflow-x-auto`.
- **`KpiDemoSeeder`** (wired into DatabaseSeeder after KpiLibrarySeeder): fills the library to **50 random KPIs**, spread across perspectives→objectives, random uom/level/category/department(+matching designation)/active, weights re-balanced so each perspective sums to its besar. Idempotent (targets total of 50). Verified: 50 KPIs, all perspective weight totals = besar.
- **LOCAL MIGRATION PRACTICE (Amirul):** don't pile up `add_*_to_table` migrations locally — fold column changes directly into the original `create_*` migration and `php artisan migrate:fresh --seed`. Done for: `kpis` (strategic_objective_id + weight), `bsc_perspectives` (description). The `kpis.strategic_objective_id` FK is added inside `create_strategic_objectives` migration (that table is created later than kpis) — column lives in create_kpis (no inline FK). Descriptions/weights seeded via BscPerspectiveSeeder/KpiLibrarySeeder.
- **PER-CATEGORY weight balancing (REWORKED 2026-06-26, Amirul):** SUPERSEDES the old "global total" weight check. Because a KPI has a `category_scope` (null=All / ceo / executive), each category's SCORECARD only gets a SUBSET (KPIs scoped `null` + that category). So a single global sum is meaningless — each category's eligible set must independently total the perspective's besar. `Kpis/Show`: `liveWeightTotal()`/`weightMatchesPerspective()` REPLACED by `categoryWeightTotal($cat)`/`categoryWeightMatches($cat)` (sum where scope IN [null,$cat]); `kpiScopes[]` (id=>scope) loaded beside `kpiWeights[]` and synced on create/edit/delete. `saveWeights()` now blocks if ANY category in `Show::WEIGHT_CATEGORIES` (`['ceo','executive']`) exceeds besar. View shows one green/red chip PER category (e.g. "CEO: 40%/40% ✓  Executive: 25%/40%"). `Kpis/Index` `rescaleKpiWeights()` fixed: was `factor = newBesar/globalSum` (wrong — global sum ≠ besar once scopes mix); now `factor = newBesar/oldBesar` applied to every KPI, which preserves each category's balance. `KpiDemoSeeder::distributeWeights()` reworked to balance per category (shared KPIs take besar/2 base, each category fills the rest); verified all 4 perspectives CEO=EXEC=besar while rawSum=1.5×besar. KpiLibraryTest now 27 (added scope-isolation, globally-balanced-but-category-unbalanced proof, per-category exceed block). Full suite 118 green. [[reference-database-schema]]
- ⏳ NOT yet: snapshot cols on scorecard_kpis (do when building KPI-setting/cascade) so renamed/deleted Library rows don't rewrite past scorecards.

## UI CONVENTION — labels & colours come from the MODEL (Locked 2026-06-25, Amirul)
NO inline `@php` colour/label maps or `category === 'ceo' ? ...` ternaries in blades. Single source of truth on the model via consts + helper methods:
- `AppraisalCycle`: `PHASES`, `STATUS_LABELS`, `STATUS_BADGES` consts + `statusLabel()`/`statusBadge()`. (Show component now uses `AppraisalCycle::PHASES`, its own PHASES const removed.)
- `User`: `CATEGORY_LABELS`/`CATEGORY_COLORS` (solid avatar bg)/`CATEGORY_BADGES` (soft badge) + `categoryLabel()`/`categoryColor()`/`categoryBadge()`.
- `Kpi`: `LEVEL_LABELS` + `levelLabel()`. `BscPerspective`: `COLORS` (by name) + `color()`.
- Blades reference `$model->statusBadge()` etc., or `\App\Models\X::CONST` for raw category strings (e.g. rosterPreview). Refactored: cycles index/show, kpis index, users index/show, departments show/add-employee, roles show. (departments/index `$palette` left — rotating by position, no model colour.) Apply this to all future modules.

## Generate Scorecards (decisions Locked 2026-06-25)
- Eligible = ACTIVE users with an appraisal-chain role (executive-director / division-head / staff) and NOT super-admin/hr-admin. Template auto-picked by `users.category`. Creates scorecard SHELL only (no KPI/competency lines — those come later).
- **Idempotent** (firstOrCreate on unique user_id+cycle_id) → re-run skips existing, picks up new hires.
- Button available in **Draft + Active** (for mid-cycle new hires). Activating with 0 scorecards = **warn, not block**. Competency lines created LATER (not at generation).
- **BUILT 2026-06-25**: on `Cycles/Show` — "Generate" button on the Scorecards card (visible when `canGenerate()` = draft/active) → confirm modal with roster preview (eligible total / new to create / breakdown by category). `generateScorecards()` does `Scorecard::firstOrCreate(user_id+cycle_id)` (idempotent, `wasRecentlyCreated` counts new), template by `users.category` via `ScorecardTemplate::pluck('id','category')`. `eligibleEmployees()` = active + whereHas appraisal roles + whereDoesntHave admin roles. Consts `APPRAISAL_ROLES`/`ADMIN_ROLES` on Show. `GenerateScorecardsTest` (4 tests). Full suite 91 green. Next: KPI Library (strategic_objectives table + snapshot cols on scorecard_kpis), then KPI setting/cascade.

## DB Design (Completed — `db-design.md`)

14 tables designed and ready:
`departments`, `users`, `appraisal_cycles`, `bsc_perspectives`, `kpis`, `scorecard_templates`, `scorecards`, `scorecard_kpis`, `scorecard_competencies`, `competency_items`, `bell_curve_targets`, `moderation_logs`, `score_overrides`, `scorecard_status_logs`

B3/B4 contingency columns already baked in — no migration needed when HR responds.

**NEW table built 2026-06-25**: `review_periods` (15th table) — the flexible review-period layer. Columns: `cycle_id` (FK cascade), `sequence`, `label`, `weight` decimal(5,2), `is_final`, `opens_at`/`closes_at`, `status` enum(upcoming/open/scored/closed), softDeletes. UNIQUE(cycle_id, sequence). `ReviewPeriod` model + `AppraisalCycle::reviewPeriods()` relation done. Migrated & verified.

**Per-period score layer built 2026-06-25** (tables 16 & 17): `scorecard_kpi_period_scores` + `scorecard_competency_period_scores`. Non-destructive design — existing `scorecard_kpis`/`scorecard_competencies` keep their score cols as the EFFECTIVE/final rolled-up value; the new tables hold per-period entries (FK to scorecard line + review_period, both CASCADE, UNIQUE per line+period). Score cols mirror the parent line. Models + relations done (`periodScores()` on lines, `kpiScores()`/`competencyScores()` on ReviewPeriod). Note: long table names hit MySQL 64-char index limit → custom short FK/index names needed. Migrated & verified.

**Factories + test + seeder done 2026-06-25**: Compliance audit (Explore agent) found all 15 pending-module models fully compliant (#[Fillable], casts, SoftDeletes match, typed relations) — ONLY gap was missing factory files. Built 11 chain factories: BscPerspective, ScorecardTemplate (+ceo/executive/support states), Kpi, CompetencyItem, AppraisalCycle (+active/closed states), ReviewPeriod (+midYear/yearEnd/open/closed states), Scorecard, ScorecardKpi, ScorecardCompetency, ScorecardKpiPeriodScore, ScorecardCompetencyPeriodScore. `ReviewPeriodTest` (6 tests, all pass) proves the chain + relationships + unique(cycle,sequence) + cascade. `AppraisalCycleSeeder` (idempotent) seeds current-year draft cycle + default half-yearly periods, wired into DatabaseSeeder after EmployeeSeeder. UPDATED 2026-06-26: periods now **50/50 weight + real dates** — Mid-Year (Jan 1–Jun 30, w50, not final) + Year-End (Jul 1–Dec 31, w50, is_final); uses `Carbon::create($cycle->year,...)` for opens_at/closes_at. Period SCORE tables intentionally NOT seeded (transactional data). Still NO factories for the 4 module-specific models (BellCurveTarget, ModerationLog, ScoreOverride, ScorecardStatusLog) — build alongside their modules.

**Cycle UI slice 1 built 2026-06-25**: Class-based Livewire (project convention — `app/Livewire/Cycles/Index.php` + `Show.php`, views in `resources/views/livewire/cycles/`, route-views in `resources/views/cycles/`, routed via Route::view/Route::get in the `role:super-admin|hr-admin` group as `cycles.index` / `cycles.show`). Sidebar "Appraisal Cycle" link was pre-stubbed for `cycles.*` — now wired to route('cycles.index') (no wire:navigate, matching sibling links). **Index**: lists cycles (year/status badge/periods count/scorecards count/opened-by) + "Open New Cycle" modal (year unique 2000-2100, creates draft, opened_by=auth, redirects to show). **Show** = dashboard: phase stepper (draft→active→appraisal→moderation→closed), review-periods card, scorecard status-counts card, and **Advance Phase** action (forward-only; stamps opened_at on →active, closed_at on →closed; PHASES const; clears computed after update). Advance is pure Option B — updates only the cycle row, never scorecards (proven by test). `CycleTest` 5 tests pass incl. Option-B proof. Theme: navy-700/gold-500/pnsb_red-500, status badge color map. **Draft period setup built 2026-06-25**: On `Cycles/Show`, while status=draft, HR configures review periods — frequency PRESETS (Annual=1→100%/Half-Yearly=2→50% each/Quarterly=4→25% each; weight split EVENLY summing to 100, NOT checkpoint 0/100 anymore — changed per Amirul; still editable per-period; last period = is_final; via `Show::PRESETS` const + `applyPreset()` which forceDeletes existing + recreates) + **preset-driven structure only — NO manual add/remove** (Amirul: frequency is fixed to annual/half/quarterly, so adding a stray period = invalid count; change frequency by re-picking a preset). **Editing moved to a DEDICATED PAGE** (`Cycles/EditPeriods`, route `cycles.periods.edit` = `cycles/{id}/periods`, view `livewire/cycles/edit-periods`): Show dashboard now just displays periods read-only + an "Edit Review Periods" button (draft only). EditPeriods = multi-row form bound to a `periods` array + `finalIndex` (radio = single-final, enforced by UI), frequency preset buttons (`PRESETS` const, even-split weights), live total indicator. **Save FORCES exactly 100%** (`weightsBalanced()` tolerance <0.01; Save button disabled in UI when off + server `addError('weightTotal')` blocks + nothing persisted). On save: forceDelete all + recreate from array (sequence=index+1, is_final=finalIndex). mount redirects to show if cycle not draft. Empty cycle defaults to half-yearly. Show keeps the activation guard (≥1 period + 1 final + weights=100). `EditPeriodsTest` (6) + `CycleTest` (9). Show stripped of all period-edit logic. **Single-final enforced** (saving is_final=true clears it on all others). **Weights must total 100%**: live indicator in Review Periods card (green ✓ at 100, red warning otherwise via `totalWeight()`/`weightsBalanced()`), swal warning dispatched on any edit/remove that breaks 100 (`warnIfWeightsUnbalanced()`), tolerance abs(total-100)<0.01. **Activation guard**: `advancePhase` to `active` blocked (swal error) unless ≥1 period AND exactly 1 is_final AND weights total 100%. Note: review_periods has softDeletes BUT unique(cycle_id,sequence) ignores deleted_at → must `forceDelete()` periods when reconfiguring to avoid unique collision. CycleTest now 12 tests (presets, draft-only, single-final, remove, activation-guard). Sequence auto-assigned (max+1) on manual add; not user-editable.

Still TODO on cycle module: **Generate Scorecards button + generation logic** (one scorecard per appraisal-chain user, template by category, idempotent), readiness snapshot, per-scorecard reopen. Then roll-up logic.

**UI CONVENTION (locked 2026-06-25): every list table has a leading "No." column + pagination.** Header `<th ... w-12">No.</th>`; cell running number = `{{ $loop->iteration + ($this->X->currentPage() - 1) * $this->X->perPage() }}`; pagination block `@if($this->X->hasPages())<div class="px-5 py-3 border-t border-gray-100">{{ $this->X->links() }}</div>@endif`. Applied to ALL list tables: Users, Designations (was already done; relabeled `#`→`No.`), Roles + Permissions (converted from `->get()`/Collection to `->paginate(10)` + WithPagination trait + `updatedSearch(){resetPage()}` + return type LengthAwarePaginator), Cycles, and the Departments/Show employees sub-table (new paginated `employees()` computed, inline actions `unset($this->department, $this->employees)`). Departments/Index is a CARD GRID (already paginated, No. N/A). All 45 affected tests pass. Use this convention for every future table (Scorecards, KPI Library, etc.).

## Blockers Status

- **B1**: ✅ RESOLVED (C5) — grade boundaries confirmed
- **B2**: ✅ RESOLVED (C7) — tier formula confirmed from Excel
- **B3**: ⏳ Pending HR — competency scoring method per category. Build proceeds with configurable `competency_method` column. Change impact documented in planning.md Section 10.
- **B4**: ⏳ Pending HR — KPI structure per category. Build follows ALP guidelines (Sokongan/Teknikal skip KPI form). Change impact documented in planning.md Section 10.

## KIV / Parked (raised by Amirul)
- ✅ **BSC big-weight editor — BUILT 2026-06-25.** On `Kpis/Index`: an "Edit BSC Weights" button (super-admin only, gated by `canEditWeights()` = `hasRole('super-admin')`) opens a modal with all 4 perspective weight inputs + live total **forced to exactly 100%** (Save disabled + server `addError('perspectiveWeights')` otherwise). On save, for each changed perspective it **AUTO-RESCALES** that perspective's KPI weights proportionally (`rescaleKpiWeights()`: factor = newBesar / currentSum, last KPI gets the remainder so the sum is exact; even-split fallback if currentSum=0) then updates the besar. `KpiLibraryTest` covers it (super-admin rescale 40→35 ⇒ KPIs 20/20→17.5/17.5, must-total-100, non-super-admin blocked). Full suite 115 green.

## Competency Module — PLAN LOCKED 2026-06-26 (Amirul, no code yet)
- **Two types** (`competency_items.type`): **Core** (General) = applies to ALL competency-based employees, no link. **Functional** = specific, linked to BOTH department AND designation via 2 NEW pivots `competency_item_department` + `competency_item_designation`. Employee's set = active Core + active Functional where (linked to their department OR their designation), **DEDUPED**.
- **Who has competency** (by category, unchanged): ceo=NONE (100% KPI) · executive (HOD + exec staff)=20% (KPI+competency) · support=100% (competency only).
- **Functional scoping = MANY-to-MANY confirmed (Amirul 2026-06-26): a functional competency can attach to MULTIPLE departments AND multiple designations** (not 1+1). Reuse one definition across all relevant roles (DRY). Pivots already support it.
- **Proficiency level DESCRIPTIONS = PER-COMPETENCY + editable (Model B — Amirul REVERSED from generic 2026-06-26).** New `competency_items.level_descriptions` JSON {level:desc} (folded into create migration, cast array). `CompetencyItem::levelDescription($lvl)` returns the item's OWN anchor else falls back to the generic `PROFICIENCY_DESCRIPTIONS` const (kept as fallback default); `levelGuide()` returns all 5. Competency **Form page** has a "Proficiency descriptions" section = 5 inputs (level 1–5) with the generic wording as placeholder; saved as JSON (null if all blank → uses generic). Self-assessment screen: REMOVED the single generic top guide; each competency row now has its OWN expandable "levels" guide (reads competencyItem level_descriptions, fallback generic) — loaded via `competencies.competencyItem:...,level_descriptions`. Seeder gives 3 core competencies (Integrity/Teamwork/Communication) tailored anchors; the rest fall back. CompetencyLibraryTest +3 (form saves anchors, fallback works, generic fallback complete). Full suite 169.
- **SCORING = 1–5 proficiency → %.** Each item rated proficiency level **1 Basic / 2 Developing / 3 Competent / 4 Advanced / 5 Expert**. Item % = (level÷5)×100 (L3=60%, L4=80%, L5=100%). `competency_score%` = AVERAGE of item %s = (avg level ÷5)×100 (`competency_method='average'`). Reconciles Excel "scale of 150" = N items×5 (e.g. 30 items, scored 90/150 = 60% → ×20% weight = 12%). final_score = KPI%×kpi_weight + competency%×competency_weight. → self_rating/manager_rating store the 1–5 LEVEL (not 0–100); UI = proficiency selector. **CHANGE: current Scorecards/Show uses 0–100 self-rating → must switch to 1–5.**
- **TIMING = at EXECUTION, after KPI approved** (NOT at draft/setup — current build does it at draft, must MOVE). Executive: KPI approved → execution → record KPI results + self-rate competencies together. **Support (no KPI): AUTO-SKIP KPI stages → straight to competency self-assessment once cycle active** (Amirul). CEO: none. **Competency assessed ONCE per cycle (NOT per-period — unlike KPI; behavioural review is annual).** self_rating/manager_rating/final_rating live on scorecard_competencies; the scorecard_competency_period_scores table stays UNUSED for competency.
- **Chain + override (C1):** self-rate then manager reviews via manager_id chain (HOD reviews staff, ED reviews HODs, CEO nothing). Manager DIRECTLY OVERRIDES competency score (manager_rating); self_rating = reference only; final_rating = manager's (manager final authority). This is the Appraisal/Point-B side.
- **Snapshot** competency title+type onto scorecard_competencies (currently links by id only) so library edits don't rewrite history — like KPI snapshot.
- **HR Competency Library UI** (sidebar "Competencies" link already stubbed): Core list (global CRUD) + Functional list (CRUD + assign department(s)/designation(s)).
- **Negative cases:** no dept/designation→Core only; functional on both dept+designation→dedupe; no Core defined→empty section can't submit; support=competency is entire score; inactive excluded; mid-cycle designation change→snapshot protects; manager-before-self→self first; missing level→can't submit.
- **D2 (competency items Core+Functional) still needs real PNSB content** for seeding — using placeholders until provided.

## KPI PRE-FILL REWORK — ✅ BUILT 2026-06-26 (Amirul; all 6 steps + exec/support UI flow; full suite 175 green)
**Shipped:** (1) schema — dropped `kpis.category_scope`; new `kpi_department`+`kpi_designation` pivot migration (`2026_06_26_120100`, mirrors competency pivots); `Kpi::departments()/designations()` belongsToMany + `isCompanyLevel()/isDepartmentLevel()/isIndividualLevel()` helpers; `Department::kpis()`/`Designation::kpis()` inverse; KpiFactory cleaned. (2) KPI Library form (`Kpis/Show` modal) — `category_scope` select REPLACED by a `level`-driven scope picker (level `department`→department checkboxes; `individual`→designation checkboxes grouped by dept; `company`→no picker); `syncScope()` attaches per level on save, edit loads existing, switching to company clears links; table "Category" col → "Scope" col (dept codes / designation short_names / "All employees"). (3) PRE-FILL — `Scorecards/Show::generateKpiLines()` runs in mount (owner + draft + isKpiBased + no lines yet): pulls active Company-level (all) + Department-level whereHas departments=user.department_id + Individual-level whereHas designations=user.designation_id, `->unique('id')`, `firstOrCreate` snapshot kpi_title/objective_name/uom/pengukuran + `kpi_weight`=library weight (suggested default). (4) `Scorecards/Setup` LOCKED — removed `selectedKpiId`/`availableKpis`/`addKpi`/`removeLine`; employee only edits weight+targets on pre-filled lines (per-perspective ≤ besar still enforced on save). (5) UNWOUND per-category balancing in `Kpis/Show` (categoryWeightTotal/Matches + WEIGHT_CATEGORIES + kpiScopes GONE → `weightTotal()`/`weightMatches()` informational per-perspective indicator, saveWeights validates 0–100 only, no besar block — library weight is just a suggested default now); REMOVED Generate hard-gate from `Cycles/Show` (kpiReadiness/kpiLibraryReady/KPI_CATEGORIES gone, pre-flight panel readiness table → plain info note, generate button only gated on roster>0). (6) Seeders — KpiLibrarySeeder attaches dept-level→all depts, individual-level→all designations; KpiDemoSeeder dropped category logic, scopes dept/individual KPIs to random depts/designations, spreads besar per-perspective. `migrate:fresh --seed` clean. Tests: KpiLibraryTest reworked (scope-sync tests replace category-weight tests; 0–100 weight validation), ScorecardSetupTest rewritten for locked pre-filled lines, ScorecardModuleTest gate-tests removed + pre-fill tests added, new `ScorecardFlowTest` (4) locks exec+support end-to-end UI (exec: both sections + pre-fill + submit + record results; support: competency-only + submit→appraisal + Results 403). [[reference-database-schema]] [[project-progress]]
**PRE-FILL WEIGHT NORMALISATION FIX — 2026-06-26 (Amirul found ketua.kewangan KPI total = 38.89%/100%):** root cause — an employee gets only a SUBSET of each perspective's KPIs (company + their dept + their designation), so the Library's raw per-KPI suggested weights summed to <besar per perspective. FIX: `Scorecards/Show::suggestedWeights($assigned)` distributes EACH perspective's besar across the employee's assigned KPIs in it (proportional to Library weight, even split fallback, last absorbs remainder → exact besar); `generateKpiLines()` uses it for `kpi_weight`. So every perspective starts = besar, total = 100% (employee can still fine-tune). Library `weight` is now just a relative proportion. SEEDER FIX: `KpiLibrarySeeder` now puts ≥1 COMPANY-level KPI in EVERY perspective (Customer→Customer satisfaction index, Internal→Compliance & governance, Learning→Staff training) — because company KPIs cascade to everyone, this guarantees EVERY KPI-based employee (incl. CEO who is company-only) covers all 4 perspectives → 100%. Verified live: ketua.kewangan (exec, Finance) = 40/40, 30/30, 20/20, 10/10 = 100%. Tests: ScorecardModuleTest `test_prefill_weights_normalise_each_perspective_to_its_besar` + updated snapshot test (sole KPI in besar-100 persp → weight 100). Full suite 176 green. **Structural guarantee: as long as each perspective has ≥1 active company KPI, all employees balance — the "check at generation" Amirul wanted is satisfied by normalisation, not a separate validator.**

**COMPETENCY PREVIEW (read-only) — 2026-06-26 (Amirul):** while an executive fills KPIs in DRAFT (before submit), the Competency Self-Assessment section was BLANK (real lines only created at execution after KPI approval). Added `Scorecards/Show::competencyPreview()` (returns applicable active Core + Functional-by-dept/designation when no real lines exist yet, via shared `applicableCompetencyItems()`) — view shows a READ-ONLY list (title + type badge + expandable 1–5 level guide + "Rate after approval" tag + blue note "rating opens once your KPIs are approved"). Does NOT create scorecard_competency rows (timing decision preserved). Test: ScorecardModuleTest `test_executive_sees_a_readonly_competency_preview_before_approval`. Full suite 177.

**OVERRIDE — CURRENT STATE & GAPS (reviewed 2026-06-26 with Amirul, before Moderation):** lives in `Appraisals/Show` (route `/approvals/{scorecard}`). Appraiser = direct manager (`user.manager_id===auth.id`: HOD→staff, ED→HODs) OR self (ED, ceo no manager); admins view-only (canReview needs isAppraiser). TWO gates: (1) **Verification** `approveKpis()` status kpi_submitted→kpi_approved — APPROVE-AS-IS, no edit/override (the planned "Point A" weight/target override is **NOT built**; score_overrides supports stage=verification but nothing writes it). (2) **Review (Point B)** `completeReview()` status appraisal→moderation = the real override. **KPI override (built+logged):** manager enters "Manager %" per line (default=employee composite); on submit `applyKpiReview()` writes an append-only `score_overrides` row (override_type=kpi, stage=appraisal, field=score, before=employee weighted, after=manager weighted, who/when) **only when changed**, then sets the line effective to manager's — keeps BOTH (C1). **Competency override (built, NOT logged):** manager rating 1–5 (default=self_rating); `applyCompetencyReview()` sets manager_rating+final_rating=manager's, self stays reference — kept ON THE LINE, **NOT** in score_overrides. Roll-up: kpi_score=Σ manager weighted, competency_score=avg(rating÷5×100), final=KPI×tpl%+comp×tpl%, grade via boundaries → moderation+log. Live preview bar. **GAPS to revisit (Amirul flagged):** (a) no Point-A verification-stage edit/override; (b) competency overrides not in the unified `score_overrides` audit log (only on line); (c) **NO override-history UI anywhere** — rows written but never displayed to manager/HR/employee; (d) `score_overrides.notes` (reason/justification) never captured in UI. Decision pending: close these gaps first OR start Moderation and revisit. [[reference-database-schema]]

**SUPPORT-STAFF UX SIGNAL — 2026-06-26 (Amirul asked how support knows it's their turn):** support (100% competency, NO KPI, NO approval step) — their assessment opens purely on **HR activating the cycle** (vs exec, which opens after HOD approves KPIs). Old draft banner wrongly said "set up your KPIs" to support. FIX (`Scorecards/Show` view, role-aware): draft banner now 3 branches — KPI-based ("set up KPIs → submit for verification; competency opens after approval"), support+canSelfAssess ("rate each competency below, then submit your self-assessment"), support+not-yet-active ("opens once HR activates the cycle"). Competency header gained a green "● Open now" badge + "…then submit below" subtitle when canAssess. Test: ScorecardFlowTest support test asserts "Open now" + the support guidance. Full suite 177. **Confirmed flow: support self-assesses in PARALLEL with execs setting up KPIs (active cycle = go); not gated behind a later phase (revisit only if Amirul wants).**

**Exec/support UI (verified, already built in Scorecard/Competency modules):** `Scorecards/Show` branches by template — KPI section gated `isKpiBased()` (CEO+Exec), competency section gated `isCompetencyBased()` (Exec+Support), submit label adapts ("Submit for Verification" KPI vs "Submit Self-Assessment" support). Support: no KPI section, competency auto-creates, submit→appraisal; Results page 403s (KPI-only). Exec: both sections, KPI submit→kpi_submitted→(manager approve)→Record Results CTA→per-period results→appraisal, competency self-assess opens at kpi_approved.

### Original plan (for reference) — PLAN LOCKED 2026-06-26 (now superseded by the BUILT note above)
**Problem:** employee currently SELECTS KPIs from a dropdown on the Setup page. Wrong — KPIs should be **PRE-FILLED** by the employee's department/designation (like Functional competencies), not picked.
**This REVERSES the 2026-06-25 "KPI scoped by level + category only, dept/designation REMOVED" decision** — dept/designation scoping comes BACK (as pivots).

**LOCKED decisions:**
- **Scoping = `level` + dept/designation links** (drop `category_scope` ceo/executive entirely). `level=company` → no links (all); `level=department` → NEW pivot `kpi_department`; `level=individual` → NEW pivot `kpi_designation`. (Mirrors functional competencies.)
- **Cascade = Option B (everything cascades DOWN):** Company KPIs → EVERYONE (all KPI-based); Department KPIs → EVERYONE in that department (incl. the HOD); Individual KPIs → employees with that designation.
- **CEO falls into `Company` level** — Company-level KPIs ARE the CEO's scorecard (the org top goals: Revenue/Profit). Company KPIs cascade to all.
- **HOD differentiation (Amirul's key concern):** HOD + Exec staff share category `executive` — category CANNOT tell them apart. Differentiate by **org role** (HOD = `department.head_user_id` / division-head) + by **designation** (Individual KPIs). With Option B, HOD and their staff share the same Department KPIs; they differ only by their designation's Individual KPIs.
- **Pre-fill scorecard per role:** CEO = Company. HOD = Company + their Department + their designation's Individual. Exec staff = same as HOD (Company + Department + Individual). Support = none (0% KPI).
- **Targets/weights (Amirul):** the Library gives the KPI (pre-filled, snapshotted); the **EMPLOYEE still fills weight + Threshold/Meet/Stretched** on the scorecard. Library `weight` = suggested default only.
- **Locked set (Amirul):** employee CANNOT add/remove KPIs — exactly the cascaded pre-filled set. Remove the Setup "Add KPI from library" dropdown + add/remove.
- **UNWIND:** the per-category weight balancing (categoryWeightTotal/categoryWeightMatches on Kpis/Show + rescale + WEIGHT_CATEGORIES) becomes OBSOLETE (KPIs no longer category-scoped) → REMOVE it; weight balance now enforced per-perspective ON THE SCORECARD (Setup page already does perspectiveWeight vs besar). The **Generate gate per-category readiness** (`kpiReadiness`/`kpiLibraryReady` on Cycles/Show) → simplify/remove (no category check anymore).
- **STAYS:** 4 BSC perspectives + besar weights, perspectives→objectives→KPIs tree, verify→record results→manager review→roll-up→grade, support no KPI, snapshot pattern.

**BUILD ORDER (staged, loop, test each):** (1) schema — drop kpis.category_scope, add pivots `kpi_department`+`kpi_designation`, Kpi model departments()/designations() + level helpers, factories; (2) KPI Library form — replace category_scope select with a level + dept/designation scope picker (like competency form); (3) **pre-fill logic** — when KPI-setting opens for a KPI-based scorecard, auto-create scorecard_kpis from matching Library KPIs (Company + employee.department + employee.designation, deduped, snapshot title/objective/uom/pengukuran + default weight); (4) lock `Scorecards/Setup` — remove add/remove, employee only edits weight+targets on pre-filled lines; (5) unwind per-category balancing in Kpis/Show + simplify Cycles/Show generate gate; (6) seeder (scope demo KPIs by level+dept/designation) + tests + migrate:fresh. Resume from (1).

## MODERATION MODULE — BUILT 2026-06-27 (bell curve, MOD1/MOD2, lock) ✅

Full appraisal spine now reaches the end: `appraisal → manager review (grade) → moderation (MOD1→MOD2) → lock → completed/closed`. Suite **191 green**.

**Locked decisions (Amirul, 2026-06-27):**
- **Two rounds, per the schema:** **MOD1 = HR Admin (per-department)** → **MOD2 = KPE/executive-director (org-wide)** → **HR locks**. (Division Heads do NOT moderate — Amirul chose "MOD1 = HR" over "Division Heads do MOD1".) Resolves **D1: HR locks final grades.**
- **Pool scope = per-department then org-wide.** Reconciled with "MOD1=HR": HR balances each department's curve (MOD1), then KPE balances the whole company (MOD2). Per-dept targets are **soft guidance** (small depts round ~0.5 → 0–1); the **real curve is enforced at MOD2 org-wide** (91 people = meaningful distribution).
- **Adjust = move the GRADE BAND, never the score.** `final_score` is the earned performance (kept for audit/appeals); moderation only re-bands `scorecards.grade`. Reads as "earned 91 (Cemerlang) → moderated to Sangat Baik". (Chose this over score-tweaking — moderation is re-banding, not re-scoring.)
- **Targets scale from % × headcount** (largest-remainder → counts sum exactly to headcount). Percentages: excellent 3.30 / very_good 17.58 / good 67.03 / satisfactory 7.69 / needs_improvement 4.40. 91 staff → exactly 3/16/61/7/4.
- **Grade keys are ENGLISH** across `scorecards.grade` + `bell_curve_targets.grade` + `moderation_logs.{before,after}_grade` (excellent/very_good/good/satisfactory/needs_improvement) — the db-design Malay enum was superseded; DB + `Scorecard::GRADE_LABELS` agree.

**Code built:**
- `BellCurveTarget`: `PERCENTAGES` const, `targetsForHeadcount()` (largest-remainder), `distribution()` counter, `syncForCycle()` (persists org-wide targets — available but NOT yet auto-called on phase entry; screens use live computation).
- `ModerationLog::record($scorecard, $round, $newGrade, $moderatorId)` — shared re-band: logs before/after grade (keeps score in before/after_score), updates grade, no-ops if unchanged. `ROUNDS` const.
- `app/Livewire/Moderation/Index.php` — cycle picker + org distribution + department list (MOD1 links) + Run-MOD2 button + **`lockGrades()`** (scorecards→`completed`+locked_at/by, cycle→`closed`, status logs). `canLock()` = moderating + HR/super-admin.
- `app/Livewire/Moderation/Session.php` — DRY single screen for BOTH rounds: `departmentId` set → MOD1 (HR), null → MOD2 (KPE). `round()`, `canAccess()` (round-gated), `isEditable()` (cycle.status==='moderation'), `reband()`. Live distribution vs target, employee table with grade dropdown.
- Views: `moderation/index` + `moderation/session` wrappers, `livewire/moderation/{index,session}` + shared `_distribution` partial (bars + target marker + over/under). Routes `moderation.index|department|org` (role `super-admin|hr-admin|executive-director`). Sidebar "Moderation" link activated.
- `ModerationTest` (8): exact-sum targets, zero headcount, sync idempotency, MOD1 re-band (keeps score + logs), MOD2 re-band, HR-blocked-from-MOD2, closed-cycle blocks re-band, HR lock closes cycle.

**Optional follow-ups (not blockers):** (1) auto-call `syncForCycle()` when a cycle advances to moderation (persist targets for audit); (2) hard-gate Lock on MOD2 being complete (currently not enforced). **NEXT MODULE: Reports & Dashboard.**

## OTHER WORK THIS SESSION (2026-06-27)

- **TEMP dev helpers (LOCAL ONLY — remove before production):** (a) login page **"Dev Quick Login"** panel — one account per role (the 6 `@pnsb.com.my` personas), click "Use" fills email+password (`fillCredentials`), env-guarded; CEO (`kpe@`) given the **Management** department (UserSeeder + live row) so every quick-login user has dept+designation. (b) **"Dev: Fill targets"** button on `Scorecards/Setup` — fills Threshold/Meet/Stretched (60/80/100 + uom), leaves weight. (c) **"Dev: Fill results"** on `Scorecards/Results` — fills the selected period's achievement % (80) + sample actual. All three local-guarded in view + method.
- **Point A verification override — BUILT.** `Appraisals/Show` at `kpi_submitted`: weight + Threshold/Meet/Stretched are now **inline-editable** (pre-filled with employee's proposed values). `approveKpis()` validates weights 0–100, then per changed field writes a `score_overrides` row (`stage=verification`, keeps before=employee/after=approved), updates the line, flips status. No edits = no override rows. AppraisalTest +3. Also fixed a **column-misalignment bug** on that page (body was missing Unit + Pengukuran `<td>`s → Pengukuran showed "—").
- **Competency Self-Assessment UI → Likert matrix.** Replaced the 1–5 dropdown with a **table**: competencies = rows, the 5 levels = column headers (label + number once), a **radio** per cell, each level column **tinted** (rose→green scale) so radios are visible; hover a cell = tooltip with that competency's level description. Read-only after submit shows filled/empty circles. Save button moved to the **bottom** of the section.
- **Competency comment + attachments (overall) — BUILT with Spatie Media Library.** Installed `spatie/laravel-medialibrary` v11. `Scorecard implements HasMedia` + `competency_attachments` collection (any type, multiple). New `scorecards.competency_self_comment` column. `Scorecards/Show`: WithFileUploads — comment textarea (saved with ratings) + multi-file upload (auto-uploads on pick, `updatedNewAttachments`) + remove + download links; gated to owner self-assess; read-only after submit. ScorecardModuleTest +3.
- **Manager Review (Appraisals/Show) improvements — BUILT.** (1) **Competency Review now uses the SAME Likert matrix** as the employee self-assessment (rows × 5 tinted level columns) — manager picks a **radio per row** (override; pre-selected at the employee's self-rating), the employee's pick is marked with a **"● self"** tag in its cell, hover = tooltip + a per-row **"levels" toggle** expands all 5 proficiency descriptions (`levelGuideFor`). (2) **KPI section shows the employee's ACTUAL recorded values** — "Employee result (actual)" column = composite % + **per-period breakdown** (actual text + achievement % per review period) so the manager assesses with evidence. New `periodBreakdown()` (eager-loads `kpis.periodScores` + `cycle.reviewPeriods`). AppraisalTest +1 (16 total).
- **Interactive bell-curve chart — BUILT (matches the PNSB `files/image.png` reference).** Replaced the column histogram with a **smooth line chart**: dashed **Target** curve + solid blue **Actual** curve + soft area fill + markers/counts, x-axis ordered **worst→best** (`Scorecard::GRADES_LOW_TO_HIGH`) so "good" peaks in the middle. **Morphs on re-band**: `Session::reband()` dispatches `bell-updated` with the new low→high counts (`orderedDistribution()`); an Alpine component **tweens the curve** via `requestAnimationFrame` (650ms easeOutCubic). Partial `livewire/moderation/_bellcurve.blade.php` (replaced `_distribution`). **Two bugs fixed:** (a) Catmull-Rom **overshoot/wiggle** on small/spiky dept data → switched to **monotone cubic (Fritsch–Carlson)** (never overshoots); (b) chart rendered **small/left & misaligned** with labels → SVG now `viewBox 0 0 100 100` + `preserveAspectRatio="none"` + `vector-effect non-scaling-stroke` (full-width stretch), dots/counts as **absolutely-positioned HTML** at the same %s so curve + dots + grade labels all align. Chart wrapped in `wire:ignore` (Alpine owns it; Livewire feeds via event). **Gotcha logged:** a `dispatch()` param named `to` collides with Livewire's `dispatch()->to()` routing — use other names.
- **TEMP `ModerationDemoSeeder` (DEV — not in DatabaseSeeder, remove before prod):** builds cycle **year 2025 in `moderation` phase** with ~20 graded scorecards in a deliberately **inflated** spread (12 excellent vs target 1, etc.) so the bell curve is clearly off-target. Idempotent (`updateOrCreate`). Run: `php artisan db:seed --class=ModerationDemoSeeder`. Use `superadmin@pnsb.com.my` to walk MOD1→MOD2→Lock.

**FLOW (how to test moderation):** scorecards must reach `moderation` status (after manager review) AND the cycle must be in the **Moderation phase** (HR advances it on Cycles/Show) → editing is read-only otherwise. **MOD1:** login `hr@` → sidebar Moderation (`/moderation`) → pick a department → change grade dropdowns (curve morphs). **MOD2:** login `kpe@` → "Run MOD2 (org-wide)". **Lock:** `hr@` → "Lock Final Grades". `superadmin@` passes every gate (easiest for testing).

## Needs Decision (Before Module Build)

- ~~D1: Who locks final grades after moderation~~ → **RESOLVED 2026-06-27: HR locks (after KPE's MOD2).**
- D2: Actual PNSB competency items (Core + Functional) — needed for DB seeding
- D3: Exact job titles per category — needed to auto-assign scoring templates
- D4: Subsidiary (anak syarikat) handling — shared system or separate instances?

## Dev Status

- ✅ Planning complete
- ✅ Flow diagram complete
- ✅ DB design complete
- 🔄 Starting development (2026-06-24)

**How to apply**: B3/B4 pending but not blocking dev start. DB design is in `db-design.md` — refer there for table structures. When suggesting architecture choices, favour the locked decisions above.

## UI / Routing Polish (2026-06-27 PM)

- **Login is the default landing page.** `routes/web.php` root `/` no longer renders `welcome` — it now `redirect()`s: `auth()->check() ? route('dashboard') : route('login')`. Guests → login, logged-in users → dashboard (avoids bouncing an authed user to a `guest`-only route). Run `php artisan route:clear` if routes are cached.
- **`flow-diagram.md` rebuilt as PER-ROLE flowcharts.** Replaced the single end-to-end diagram with **5 role sections** (CEO/KPE, HOD/Ketua Bahagian, Executive/Eksekutif, Support/Sokongan, HR Admin), each = **pseudocode block + its own Mermaid `flowchart TD`** (same dark `classDef` palette). Kept the shared Glossary, a new Role→Category→Weighting quick-map, the Bell-Curve table, and the Escalation summary. Flows are code-grounded (status flow `draft→kpi_submitted→kpi_approved→appraisal→moderation→completed→locked`; Support fast-tracks `draft→appraisal`; CEO self-appraises; MOD1=HR per-dept, MOD2=KPE org-wide; lock=HR).
- **Super-admin sidebar — Assessments section HIDDEN.** Super-admin administers the system and is **not appraised**, so the whole "Assessments" block (My Scorecard + the role-gated Self Approval / Approvals) in `resources/views/layouts/sidebar.blade.php` is now wrapped in `@unlessrole('super-admin') … @endunlessrole`. Scoped to **super-admin only** — `hr-admin` still keeps the page (also an admin, but left as-is unless Amirul says otherwise). NOTE: scorecard *generation* already excluded both admins (`Cycles/Show::eligibleEmployees()` `whereDoesntHave ADMIN_ROLES`); this was purely the sidebar menu visibility, not generation.
- **Scorecards filter dropdown overlap — FIXED.** On `/scorecards` the Cycle `<select>` used `px-3` (12px right padding) so the browser's native arrow overlapped the year text ("Cycle 2026"). Both filter selects in `resources/views/livewire/scorecards/index.blade.php` changed to `pl-3 pr-8` to reserve arrow space. Tailwind utility — needs `npm run dev`/`build` if not already compiled.

## HR-as-employee model — two-account pattern (Confirmed 2026-06-27, Amirul)

Extends the "HR not assessed" decision. If a real HR person must BOTH run the system AND be appraised, use **two separate accounts** (NOT one account with two roles):
- **Admin account** (`hr-admin`) → opens cycles, MOD1, locks grades. No scorecard.
- **Employee account** (`staff` or, for the HR head, `division-head`, category `executive`/`support`, `manager_id` → their supervisor) → carries the scorecard, assessed via the normal chain.
- The person uses the admin login to *administer*, the employee login to *be assessed* — keeps separation of duties intact ("can't lock your own grade").
- **Why not one account with both roles?** `Cycles\Show::eligibleEmployees()` excludes anyone holding an admin role (`whereDoesntHave ADMIN_ROLES`), so a combined account wouldn't be assessable without rewriting that filter AND it blurs the conflict-of-interest line. Two accounts = zero code change. Still a future/optional path, not built now.

## Reports & Dashboard Module — BUILT (2026-06-27 PM)

Built slice-by-slice, each slice tested. **Full suite 201 green** (was 192; +9 new). Stack conventions reused: class-based Livewire + wrapper view + `Route::view`; hand-built SVG+Alpine bell-curve partial (`livewire/moderation/_bellcurve.blade.php`); `BellCurveTarget::distribution()`/`targetsForHeadcount()`; grade/status badge consts.

- **Role-aware Dashboard** (`app/Livewire/Dashboard/Index.php` + `livewire/dashboard/index.blade.php` + `_own-scorecard.blade.php`; `dashboard.blade.php` now a thin wrapper). Branches: **Admin/HR/KPE** (`super-admin|hr-admin|executive-director`) → org stat cards (appraised headcount, current cycle+phase, pending submissions, completed) + grade-distribution curve; **HOD** (`division-head`) → "Awaiting My Review" (subordinate cards at `kpi_submitted`/`appraisal`) + team progress + own scorecard; **Staff** → own scorecard (grade revealed only when completed/locked).
- **Reports page** (`app/Livewire/Reports/Index.php` + `reports/index.blade.php` wrapper + `livewire/reports/index.blade.php` + `_breakdown.blade.php`). Route `Route::view('reports','reports.index')->name('reports.index')` in the `role:super-admin|hr-admin|executive-director` group (matches the "Reports & Logs" sidebar gating — `division-head`/`staff` get 403). Sidebar "Reports" dead `href="#"` → wired to `route('reports.index')`. Features: cycle selector → bell-curve + **final-grade table** (filter by department + category, sorted by final_score desc) + **breakdown by department & by category** (grade counts, reuses `_breakdown` partial) + **moderation audit** (MOD1/MOD2 before→after grade, moderator, when).
- **Export**: **CSV** = `Reports\Index::exportCsv()` streams the FILTERED final-grade report (`response()->streamDownload`, `final-grades-{year}.csv`). **Print/PDF** = `window.print()`; sidebar + topbar get `print:hidden`, `layouts/app.blade.php` gets `print:block/h-auto/overflow-visible/p-0` so content reflows, reports view has a `hidden print:block` report header. New `print:*` Tailwind utilities → ran `npm run build`.
- **Tests**: `tests/Feature/DashboardTest.php` (3) + `tests/Feature/ReportsTest.php` (6 — access control for the 3 roles + 403 for staff/division-head, table, dept filter, breakdowns+audit, CSV stream). Also updated default `ExampleTest` (root `/` now asserts redirect to login, not 200 — consequence of the login-default-page change earlier today).
- **Manual test data**: ran `ModerationDemoSeeder` → cycle 2025 (moderation) with 18 inflated graded scorecards; login `superadmin@` to view Dashboard curve + `/reports`.

## Dashboard Enrichment + Flow-Test Seeder + DB-Design Refresh (2026-06-29)

- **Admin Dashboard — 4 new widgets** (`app/Livewire/Dashboard/Index.php` + `livewire/dashboard/index.blade.php`). Brought the admin view up to the PMS+ reference density, all driven by REAL data (decided with Amirul: build these 4, **skip** the month-by-month trend chart since we only have yearly cycles — no monthly snapshots to back it honestly). Widgets (admin/HR/KPE branch only; HOD/staff untouched):
  - **Department Performance table** — `departmentPerformance()`: scorecards `join users join departments`, grouped, avg `kpi_score`/`competency_score`/`final_score` + headcount, best dept first, status badge via `Scorecard::gradeForScore(avg_final)`.
  - **Top Performers** — `topPerformers()`: top 5 by `final_score` desc, initials avatar + dept + score.
  - **KPI by Category donut** — `kpiByCategory()`: `BscPerspective::withCount(active kpis)`. **Hand-rolled SVG donut** (no chart lib — matches the existing bell-curve convention), stacked `stroke-dasharray`/`stroke-dashoffset` arcs, `-rotate-90` to start at top. Hex map: Financial #c0392b, Customer #2563eb, Internal Business Process #d4a017, Learning & Growth #16a34a.
  - **Recent Activity** — `recentActivity()`: latest 6 `ScorecardStatusLog` with `scorecard.user` + `changedBy`, "X moved to {status}" + time-ago.
  - Scores shown on our **0–100 scale** (not PMS+'s 0–5). Every widget has an empty state. Test: `DashboardTest::test_admin_dashboard_shows_department_and_category_widgets`. **Full suite 202 green** (was 201; +1). Tailwind `bg-navy-300`/`divide-gray-50`/etc → ran `npm run build`.
- **`FlowTestSeeder`** (`database/seeders/FlowTestSeeder.php`) — minimal end-to-end test dataset: **exactly ONE active user per role**, wired into the `manager_id` chain (CEO→HOD→exec/support), + reference data (roles, BSC, templates, depts, designations, competency items, KPI library) + **draft cycle** (the true start of the flow — leaves 0 scorecards so Amirul generates them via UI). Logins (all `password`): `superadmin@` (super-admin), `kpe@` (executive-director/CEO), `ketua.kewangan@` (division-head/HOD), `eksekutif@` (staff/executive), `sokongan@` (staff/support). Run: `php artisan migrate:fresh` then `php artisan db:seed --class=FlowTestSeeder`. **`super-admin` is whitelisted in BOTH moderation rounds** (`Moderation\Index:89`, `Moderation\Session:48-49` — MOD1=hr-admin|super-admin, MOD2=executive-director|super-admin) + lock, so these 5 users can walk the ENTIRE flow without a separate HR login.
- **`db-design.md` REWRITTEN to match the live schema (2026-06-29)** — the old doc was the 2026-06-24 planning draft and had drifted hard. Verified against MySQL `information_schema`. Key corrections: roles via **Spatie Permission** (no `users.role` column); enums anglicised (`category` ceo/executive/support, grades excellent…needs_improvement); added the 9 real tables the draft missed (`designations`, `strategic_objectives`, `review_periods`, `scorecard_kpi_period_scores`, `scorecard_competency_period_scores`, 4 scoping pivots, Spatie `media`); documented the **snapshot architecture** (scorecard_kpis/competencies copy the definition + nullable soft-link) and **period-based scoring flow** (period scores → line → scorecard → grade). Real columns: `departments.name/code`, `kpis.title/weight/pengukuran`, `scorecards.competency_self_comment`, `score_overrides.stage/field`, renamed `tier_score`/`weighted_score`.
- **Results page guide-text contrast bug FIXED** (`livewire/scorecards/results.blade.php:23`). The "Guide — Threshold 60 · Meet Target 80 · Stretched 100" line was `text-gold-300`, but the Tailwind config only defines gold **400/500/600** — `gold-300` emits NO color rule, so the text fell back to default dark gray and vanished on the navy header. Changed to a **gold legend chip**: `bg-gold-400 text-navy-800` rounded-full pill. Ran `npm run build`. **Watch-out**: only gold-400/500/600, navy-50…900, pnsb_red-500/600 exist in `tailwind.config.js` — referencing an undefined shade silently renders colorless text.

## Cycle-Close Back-Door FIXED (2026-06-29) ✅

**Bug (Amirul found):** a cycle showed status `closed` but the HOD/Executive scorecards still read **"Pending moderation"** because they were never locked. Root cause = **two ways to close a cycle**, only one safe:
- **Safe path** `Moderation\Index::lockGrades()` (`app/Livewire/Moderation/Index.php:93`) — one DB transaction: every graded scorecard → `completed` (+`locked_at`/`locked_by` + `ScorecardStatusLog`) **then** cycle → `closed`. Gated by `canLock()` (moderation phase + scorecards present + super-admin/hr-admin).
- **Unsafe back-door** `Cycles\Show::advancePhase()` (`app/Livewire/Cycles/Show.php:130`) — the generic phase stepper walked `moderation → closed` and stamped `closed_at` but **never touched scorecards** (its own modal admits "It does not change individual scorecards"). Closing this way left cards stuck pre-`completed`. The dashboard shows "Pending moderation" for any card not `completed`/`locked` (`_own-scorecard.blade.php:30`) → stuck forever.

**Fix (prevention only — make Lock Final Grades the SOLE exit from moderation):**
- **Layer 1 — backend guard:** `advancePhase()` now early-returns with a swal info ("Close from Moderation → use Lock Final Grades") when `next === 'closed'`. Removed the dead `closed_at` branch (it's owned by `lockGrades()`). Cycle can no longer reach `closed` via this method even on a forged request.
- **Layer 2 — UI redirect:** in `cycles/show.blade.php`, when `$next === 'closed'` the gold "Advance to Closed" button is replaced by a purple **"Finish in Moderation → Lock Final Grades"** link to `route('moderation.index')` (`@if($next === 'closed') … @elseif($next) … @else`). Only the correct close road is visible. **Needs `npm run build`** to show.
- `lockGrades()`/`canLock()` untouched — already correct.
- **Test:** `CycleTest::test_a_cycle_in_moderation_cannot_be_closed_via_advance_phase` — asserts cycle stays `moderation`, `closed_at` null, scorecard untouched. **Pint passed, 18/18 CycleTest+ModerationTest green.**

**Out of scope (chosen by Amirul "prevention only"):** (a) repairing the ALREADY-broken cycle is a separate one-off — reopen it to `moderation` (manual `status` flip), then HR clicks Lock Final Grades so cards lock via the proper logged path; (b) a guarded in-app "Reopen Cycle" action (recover a mis-closed cycle without tinker) — NOT built, parked.

## Multi-period cycle "hanging" flow — reviewed, left as-is + KIV logged (2026-06-29)

Amirul asked whether 2-/4-period cycles leave the staff hanging (key in Period 1 → done → must return later for the next period). Traced it: **NOT a bug — intentional but silent.** One status for the whole cycle; `Scorecards/Results::finalize()` is gated by `allPeriodsScored()`, so the card legitimately waits at `kpi_approved` between periods until every KPI in every period is filled, then moves to `appraisal`. **Decision: leave behaviour as-is.** Three rough edges parked as KIV (no code) — see `watchouts.md` → "Multi-period cycle 'hanging' flow": **A** no "come back" nudge on the staff dashboard, **B** `periodLocked()` ignores `opens_at` (future periods fillable early), **C** `review_periods.status` enum is decorative (never flipped).

## Audit Trail Module (2026-06-30) ✅

General-purpose model audit trail via **`owen-it/laravel-auditing` v14** (Amirul chose the package + "all data" scope). Branch `feat/audit-trail`, **issue #3**, 2 commits, **NOT merged** (PR into `dev` not opened yet). Fills the admin/config blind spot — the appraisal pipeline was already hand-audited (`moderation_logs`, `score_overrides`, `scorecard_status_logs`), but config edits (KPI weights, BSC weights, competency items, user/role changes) were silent.

**Round 1 — recording (`daf5cab`):**
- Installed package, published `config/audit.php` + `audits` migration (`2026_06_30_090609`). Resolves clean on Laravel 13 / PHP 8.4.
- `Auditable` interface + trait added to **16 models**: AppraisalCycle, BellCurveTarget, BscPerspective, CompetencyItem, Department, Designation, Kpi, ReviewPeriod, Scorecard, ScorecardCompetency, ScorecardCompetencyPeriodScore, ScorecardKpi, ScorecardKpiPeriodScore, ScorecardTemplate, StrategicObjective, User. Pattern: `implements AuditableContract` (aliased) + `use Auditable` trait. (`Auditable` listed first in trait `use`.)
- **SKIPPED on purpose** (already append-only audit records — auditing them is recursive): `ModerationLog`, `ScoreOverride`, `ScorecardStatusLog`.
- `config/audit.php`: global `exclude => ['password','remember_token']` (secrets never logged); `console => false` (default) so **seeders/tinker DON'T record audits** — IMPORTANT gotcha (see below). `timestamps => false`.
- `AuditTrailTest` (4): create/update/delete records, secrets excluded, log tables stay non-auditable.

**Round 2 — viewer (`<2nd commit>`):**
- **`/audit-trail`** Livewire page (`App\Livewire\Audit\Index`), route gated **`role:super-admin` ONLY** (separation of duties: log can record HR's own actions, so hr-admin must NOT read it — tighter than Reports which is super-admin|hr-admin|executive-director). Sidebar link under "Reports & Logs", `@role('super-admin')`.
- Paginated (15) filterable table: search actor (name/email/employee_no via `whereHasMorph` on `user`), filter by model (`auditable_type`) / event / user. Color-coded event badges, inline `old → new` preview (first 4 fields + "+N more").
- **Per-record history slide-over** — `viewHistory($type,$id)` sets `historyType`/`historyId`, `recordHistory()` computed returns that auditable's full timeline newest-first.
- `AuditViewerTest` (5): access control (super-admin OK, other roles 403, guest→login), listing+filter, timeline.

**Gotchas hit & fixed:**
- **Console auditing OFF by default** → tests must `config(['audit.console' => true])` in `setUp()`, else NO audits record (PHPUnit runs in console). Same reason seeders stay quiet.
- **Cross-test state leak** unmasked a strict-error during blade render ("Undefined array key" — passed in isolation, failed in full suite). Fix: moved change-formatting OUT of the Blade into a component method `changeRows(Audit): array` that pre-stringifies values (explicit null defaults) → view never touches raw audit-value keys. Cleaner + deterministic.
- Used `bg-amber-50/text-amber-600` for the `restored` badge (NOT `gold-50` — gold only has 400/500/600 per `tailwind.config.js`).

**Status:** Pint clean, `npm run build` done, **full suite 253 green** (was 248 pre-feature; +9 audit tests, -4 because... net 244→253). Verified on dev DB: ran a tinker edit-and-revert as super-admin (`config audit.console=true`) → 6 audit rows, actor "Super Admin", old/new captured, seed data unchanged. URL `http://kpi_pnsb.test/audit-trail`. **dev `audits` table is otherwise empty** (console/seeders skip auditing) — page blank until real UI edits happen.

**Parked / next:** open PR into `dev`; optional follow-ups — tag-based audit grouping, retention/pruning, audit the relationship-sync pivots (kpi_department etc. aren't auto-audited), CSV export of the log.

### Round 3 — auth auditing (login/logout/failed) (2026-06-30)

Amirul asked "did the audit catch login user?" — it didn't (owen-it only hooks Eloquent model events). Added it:
- **`App\Listeners\LogAuthEvents`** writes auth activity into the same `audits` table. Login & logout attach to the user (`auditable` = that user → shows in their per-record timeline); **failed logins are system-level** (user/auditable null) carrying the attempted email + IP. Events: `login` / `logout` / `login_failed`, `tags='auth'`.
- **Migration `…094358_make_audits_auditable_nullable…`** makes `audits.auditable_type`/`auditable_id` nullable (owen-it's `morphs()` made them NOT NULL; failed logins have no model).
- Listener mirrors the package's console rule (`shouldAudit()`) so seeders/tinker/tests don't record auth noise.
- Viewer updated: `login`/`logout`/`login_failed` filter options + badges (emerald/gray/red), and model-less rows render "Authentication / system" with no history button; `modelOptions` excludes null types.
- **GOTCHA (important):** Laravel 11+ **auto-discovers** listeners in `app/Listeners` (registers `handle*` methods by their type-hinted event). I ALSO added manual `Event::listen` in AppServiceProvider → **double registration → every event logged twice**. Caught it on dev (3 events → 6 rows). Fix: **remove the manual `Event::listen`**, rely on auto-discovery (AppServiceProvider::boot is back to a comment). `AuthAuditTest` now asserts **exactly 1 row per event** to guard the regression.
- `AuthAuditTest` (4): login/logout/failed recorded correctly + not recorded when `audit.console` off. **Full suite 257 green** (253 → +4). Verified on dev (1 login + 1 failed row).

### Round 4 — searchable user filter combobox (2026-06-30)

Amirul: the audit-index user filter is a plain `<select>` that won't scale with many users — wanted Select2. **Decided AGAINST Select2** (would add jQuery + select2 = 2 new deps, and Select2 fights Livewire's DOM morphing — needs re-init hooks). Built a **native Alpine + Livewire searchable combobox instead** (zero deps, Amirul approved): trigger button shows the selected actor (× to clear) + a panel with an inline search input. `userSearch` filters `userOptions` **server-side** (name/employee_no) capped at **20 results** so the DOM never holds thousands of options; `selectUser`/`clearUser` actions drive `filterUser`; `userSearch` narrows only the option list, not the table. `AuditViewerTest` +1 (search-narrows-options + select/clear — asserts on the `userOptions` collection, NOT `assertSee`, because the actor's name also appears in the audit table's actor column). **Full suite 258 green.** Stack has no jQuery/Select2/TomSelect — Livewire 4 + Alpine + Tailwind + SweetAlert2 only. Branch now **4 commits**, still unmerged.

---

## PNSB Visual Rebrand + UI Overhaul (2026-06-30) — branch `feat/pnsb-rebrand`, 8 commits, NOT pushed / no PR yet

Big visual + UX session. Studied the official corporate site **pnsb.com.my** (WordPress/Elementor "biznax" theme) by pulling its **live CSS** to get the *real* brand tokens, then rebranded the whole admin app + added a landing page, UI improvements, and error pages. All on `feat/pnsb-rebrand` off `dev`. **GitHub issue was blocked by the safety classifier** (external write not user-initiated) — branch made locally; issue still not created.

**Confirmed PNSB brand tokens (from live site CSS — source of truth):**
- Navy: primary `#1D344E` · dark `#0B1B2E` · darkest `#00091D`
- Gold (ochre, NOT yellow): `#D4873A` · Red (vivid): `#E40414` / dark `#A90707`
- Teal accent `#5EEAD4` (== Tailwind `teal-300`) · wine `#8E2C4D` · steel `#BCC3C5`
- **Swoosh gradient** (from supplied logo): `#EF8534 → #EC4423 → #EC1F25` → token `bg-pnsb-swoosh`
- **Fonts: DM Sans (body) + Heebo (headings)** via Bunny Fonts (site also uses paid GT Walsheim Pro — skipped, Heebo covers it). Was Figtree before.

**Commit 1 — tokens/components/motif/logo:** rewrote `tailwind.config.js` navy/gold/pnsb_red to full **50–900 ramps** at true hues (fixed a latent bug — `gold-50`/`pnsb_red-50..400` were referenced by badge constants but undefined → blank backgrounds). Added `@layer components` in `app.css` (`.btn-primary/-gold/-danger/-ghost`, `.card`, `.card-header`, `.stat-card`, `.input`, `.badge`) + `h1–h6 { @apply font-heading }`. New `<x-pnsb-pattern>` (low-poly triangle SVG motif, navy/light variants). Brand pack supplied by Amirul at `public/image/pnsb-brand-pack/` → copied logo to `public/images/pnsb-logo{,-white}.svg` + favicons to `public/` root; `<x-application-logo variant="navy|white">` switches per surface (white wordmark for dark navy bg — navy logo is invisible on navy). Sidebar + login hero rebranded (login got the motif + **SPIKE values strip**: Sinergi/Profesional&Proaktif/Integriti/Kelestarian/Empati). Re-skinned Breeze buttons/input off indigo.
**Commit 2 — view migration:** deterministic literal-substring swap of repeated inline utilities → component classes across 41 views (`.card` ×92, `.stat-card` ×6, `.btn-primary` ×12). Cosmetic-neutral.
**Commit 3 — landing page:** `/` now serves a public **landing.blade.php** to guests (navy hero + motif + logo + SPIKE + feature cards + CTA → login); authed users still redirect to dashboard. Route named `landing`. `ExampleTest` rewritten (guest sees landing 200 / authed → dashboard) — needed `RefreshDatabase`.
**Commit 4 — a11y + buttons:** gold text on light → `gold-700` (WCAG AA; gold-500/400 stay for fills + on-navy); `aria-label`/`aria-expanded` on icon-only topbar buttons + search; skip-to-content link + `#main-content`; 12 modal confirm buttons → `.btn-danger`.
**Commit 5 — UX:** **global Livewire loading bar** (`#global-loading-bar` + `app.js` `Livewire.hook('commit')`); `<x-empty-state>` (applied to scorecards/approvals); wrapped the one un-wrapped table (`cycles/index`) in `overflow-x-auto`; **`<x-cycle-progress>` stepper** on scorecard show (Draft→KPI→Approved→Appraisal→Moderation→Final); **dashboard pending-action nudges** (managers: "N awaiting verification" → approvals; staff: draft scorecard → their card).
**Commit 6 — error pages:** branded `resources/views/errors/{401,402,403,404,419,429,500,503}.blade.php` + shared `errors/layout.blade.php` (navy + motif + logo, self-contained — no DB/auth/JS so it renders during failures). NB: custom **500 only shows when `APP_DEBUG=false`** (Ignition handles it locally).
**Commit 7+8 — @php audit/refactor:** audited every `@php` block. Moved 3 inline **label/colour maps** to their model home (matching the existing `STATUS_BADGES`/`GRADE_LABELS` convention): proficiency tint `$levelBg` (was triplicated) → **`CompetencyItem::PROFICIENCY_LEVEL_BG`**; grade bar colours → **`Scorecard::GRADE_BAR_COLORS`**; audit event→badge closure → **`Audit\Index::eventBadge()`** method. Rest of `@php` justified (computed-prop aliases, component method calls, model-constant refs, component-internal class maps, decorative palettes, `use Str` for formatting).

**Descoped (with reasons):** Heroicons/Blade-Icons dep (needs approval per CLAUDE.md); icon-component standardization (inline SVGs already consistent `stroke-2`/24-viewBox — would be churn/dead-code).

**Status:** **suite ~280–281 green.** Known **pre-existing flake**: `AuditViewerTest` user-search/combobox tests fail intermittently in the *full* run (test-ordering/state leak) but pass **7/7 in isolation** — NOT caused by this work; worth a separate fix (likely shared seeded-user/autoincrement ordering). Pint + `npm run build` clean throughout. UI rating went ~7.5 → ~8.7 (a11y 5.5→8). **Next:** push `feat/pnsb-rebrand` + open PR into `dev` (external GH writes may need Amirul to run/approve); optionally create the issue; eyeball in-browser (Ctrl+F5 — favicon caches hard).
